function send(){ //nom de la fonction
  var firstNumber = document.getElementById('premierNombre').value;
  var secondNumber = document.getElementById('secondNombre').value;
  var result = parseInt(firstNumber) * parsefloat(secondNumber);
  alert(result);
}
